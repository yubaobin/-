'use strict';

angular.module("app").controller('AppCtrl',['$rootScope', '$scope', '$localStorage', '$window', '$state', 'toaster',
	function($rootScope, $scope, $localStorage, $window, $state, toaster){
		
}])